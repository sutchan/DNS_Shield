schema: spec-driven

# Project context
# This is shown to AI when creating artifacts.
context: |
  Tech stack: TypeScript, React, Next.js, Tailwind CSS, Jest
  We use conventional commits
  Domain: DNS-based ad blocking
  Project structure follows Next.js App Router conventions
  We use SemVer for versioning
  We maintain a single source of truth in domains.txt
  We support multiple output formats: dnsmasq, hosts, and AdGuard
  We follow strict file header format: <root_relative_path> <version>
  We also maintain adguard_whitelist.txt for whitelist rules
  We have additional configuration files for deployment and testing

# Per-artifact rules
rules:
  proposal:
    - Keep proposals under 500 words
    - Always include a "Non-goals" section
  tasks:
    - Break tasks into chunks of max 2 hours
    - Include testing tasks for all functionality changes
  spec:
    - Maintain consistency with actual project structure
    - Update dependency versions to match package.json
    - Include all supported router platforms
  checklist:
    - Include all required files and configurations
    - Verify all generated file formats
    - Ensure router compatibility testing
