# 测试指南

本指南将帮助你了解如何测试 DNS Shield 项目，确保项目功能正常工作。

## 测试类型

### 1. 功能测试

#### 1.1 Web 管理工具测试

1. **启动开发服务器**：
   ```bash
   npm run dev
   ```

2. **访问 Web 管理工具**：
   - 打开浏览器，访问 `http://localhost:3000`

3. **测试输入功能**：
   - **URL 导入**：从预设源 URL 导入域名列表
   - **预设源**：测试 AdGuard、EasyList、NeoHosts 等预设源
   - **手动编辑**：在文本框中手动添加/删除域名
   - **本地加载**：上传本地 `domains.txt` 文件

4. **测试输出功能**：
   - **Dnsmasq 格式**：生成并检查格式是否正确
   - **Hosts 格式**：生成并检查格式是否正确
   - **AdGuard 格式**：生成并检查格式是否正确
   - **混合输出**：同时生成多种格式
   - **头部注释**：检查是否自动生成项目信息头
   - **一键下载**：测试下载功能
   - **剪贴板复制**：测试复制功能

5. **测试选项设置**：
   - 修改项目名称、版本号
   - 修改 IPv4/IPv6 目标 IP
   - 测试添加头部注释、阻止 IPv6、自动去重、移除通配符前缀等选项

#### 1.2 规则文件测试

1. **生成规则文件**：
   - 使用 Web 管理工具生成 `dnsmasq.conf`、`hosts.txt` 和 `adguard.txt` 文件

2. **检查文件格式**：
   - **Dnsmasq 格式**：检查是否为 `address=/domain/0.0.0.0` 格式
   - **Hosts 格式**：检查是否为 `0.0.0.0 domain` 格式
   - **AdGuard 格式**：检查是否为 `||domain^` 格式

3. **检查文件内容**：
   - 验证域名数量是否正确
   - 验证头部注释是否完整
   - 验证白名单规则是否正确处理
   - 验证注释域名是否正确处理
   - 验证自定义 DNS 规则是否正确处理

### 2. 兼容性测试

#### 2.1 浏览器兼容性测试

测试 Web 管理工具在以下浏览器中的表现：

- Chrome 80+
- Firefox 75+
- Safari 13+
- Edge 80+

测试内容包括：
- 页面加载是否正常
- 功能是否完整可用
- 响应式布局是否正常
- 交互是否流畅

#### 2.2 路由器兼容性测试

测试生成的规则文件在以下路由器/固件中的表现：

| 路由器/固件 | 测试内容 |
|------------|----------|
| Merlin (华硕) | Dnsmasq 格式规则是否正常工作 |
| OpenWrt | Dnsmasq 格式规则是否正常工作 |
| 小米路由器 | Hosts 格式规则是否正常工作 |
| 华硕路由器 | Hosts 格式规则是否正常工作 |
| TP-Link | Hosts 格式规则是否正常工作 |

测试步骤：
1. 登录路由器管理后台
2. 按照 README 中的说明导入规则文件
3. 重启路由器或相关服务
4. 测试广告是否被正确过滤

### 3. 性能测试

#### 3.1 Web 管理工具性能

- **加载速度**：测试页面加载时间
- **响应速度**：测试操作响应时间
- **内存使用**：测试内存占用情况
- **大型列表处理**：测试处理大量域名时的性能

#### 3.2 规则文件性能

- **文件大小**：检查生成的规则文件大小
- **解析速度**：测试路由器解析规则的速度
- **内存占用**：测试路由器加载规则后的内存占用

### 4. 安全测试

- **输入验证**：测试输入域名的验证
- **XSS 防护**：测试是否存在 XSS 漏洞
- **CSRF 防护**：测试是否存在 CSRF 漏洞
- **敏感信息**：检查是否泄露敏感信息

## 测试工具

### 1. 浏览器开发者工具

- **Chrome DevTools**：用于调试和性能分析
- **Firefox Developer Tools**：用于调试和性能分析

### 2. 网络测试工具

- **Pingdom**：测试网站加载速度
- **GTmetrix**：测试网站性能

### 3. 广告测试工具

- **AdBlock Tester**：测试广告过滤效果
- **EasyList Test**：测试规则兼容性

## 测试流程

### 1. 开发阶段测试

- 每次修改代码后进行基本功能测试
- 确保修改不会破坏现有功能
- 测试边界情况和异常情况

### 2. 发布前测试

- 完整的功能测试
- 浏览器兼容性测试
- 路由器兼容性测试
- 性能测试
- 安全测试

### 3. 定期测试

- 每2周进行一次规则有效性测试
- 定期测试浏览器兼容性
- 定期测试路由器兼容性

## 测试报告

创建测试报告，记录测试结果和发现的问题：

| 测试类型 | 测试项 | 预期结果 | 实际结果 | 状态 | 备注 |
|----------|--------|----------|----------|------|------|
| 功能测试 | URL 导入 | 成功导入域名列表 | | | |
| 功能测试 | 预设源加载 | 成功加载预设源 | | | |
| 功能测试 | 规则生成 | 生成正确格式的规则文件 | | | |
| 兼容性测试 | Chrome | 正常工作 | | | |
| 兼容性测试 | Firefox | 正常工作 | | | |
| 兼容性测试 | Safari | 正常工作 | | | |
| 兼容性测试 | Edge | 正常工作 | | | |
| 路由器测试 | Merlin | 规则生效 | | | |
| 路由器测试 | OpenWrt | 规则生效 | | | |
| 路由器测试 | 小米路由器 | 规则生效 | | | |
| 性能测试 | 页面加载 | < 3秒 | | | |
| 性能测试 | 规则生成 | < 5秒 | | | |

## 问题排查

### 1. Web 管理工具问题

- **页面加载失败**：检查网络连接、服务器状态
- **功能不可用**：检查浏览器控制台错误、代码语法错误
- **规则生成失败**：检查输入域名格式、浏览器兼容性

### 2. 规则文件问题

- **格式错误**：检查生成的文件格式是否正确
- **规则不生效**：检查路由器配置、规则文件路径
- **性能问题**：检查规则文件大小、路由器性能

### 3. 路由器兼容性问题

- **规则不生效**：检查路由器支持的格式、配置方法
- **性能下降**：检查规则文件大小、路由器性能
- **功能冲突**：检查与其他规则或服务的冲突

## 最佳实践

- **测试环境隔离**：在独立的环境中进行测试
- **测试数据备份**：测试前备份原始配置
- **测试用例记录**：记录测试用例和结果
- **回归测试**：每次修改后进行回归测试
- **自动化测试**：尽可能自动化测试流程

## 结论

通过严格的测试流程，可以确保 DNS Shield 项目的质量和可靠性。测试不仅可以发现和修复问题，还可以提高项目的整体质量和用户体验。

定期进行测试，特别是在版本更新和功能添加后，可以确保项目始终保持最佳状态。